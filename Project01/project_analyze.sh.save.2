cd ..
#!/bin/bash
# Bash Script

# Asks for user input on what they want to do.
#echo -n "What do you want to do? (File Count)"
#read action
#echo -n "You want to ${action}!"

#File Count
count_sh=$(ls -l | grep *.sh | wc -l)count_sh=$(ls -l | grep *.sh | wc -l)count_sh=$(ls -l | grep *.sh | wc -l)


echo $count_sh
